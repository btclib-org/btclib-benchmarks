.venv/
.venv-*/
__pycache__/
*.py[cod]
.coverage
.pytest_cache/
.ruff_cache/
.mypy_cache/
build/
dist/
*.egg-info/

# the shared input pool: re-derivable from two constants in
# src/btclib_benchmarks/_inputs.py, and built once so the second run on
# a machine starts at the numbers
.inputs/
