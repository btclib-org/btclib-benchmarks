# 3.13 and not the 3.14 the rest of this org pins, and the reason is a
# comparand rather than a preference: coincurve and secp256k1, the two
# wrappers scripts/libsecp256k1_wrappers.py exists to compare, publish
# wheels up to cp313 and no further, and neither builds from source
# without pkg-config and a toolchain. A benchmark that cannot install
# its comparands measures nothing.
# Raise this the moment both publish a cp314 wheel; requires-python in
# pyproject.toml stays at the 3.10 floor either way, that being what the
# scripts themselves need
3.13
